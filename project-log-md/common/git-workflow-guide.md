# Git Workflow & Best Practices: HR-IMS
**Timestamp:** 2026-01-12 16:20:21

เพื่อให้การบันทึกประวัติโค้ดเป็นระบบและปลอดภัย แนะนำให้ทำตามขั้นตอนและมาตรฐานดังนี้ครับ:

## 🛡️ Safe Workflow (ขั้นตอนที่ปลอดภัย)

1.  **Check Status**: ตรวจสอบไฟล์ที่มีการเปลี่ยนแปลงก่อนเสมอ
    ```bash
    git status
    ```
2.  **Review Changes**: ดูโค้ดที่แก้ไปว่าถูกต้องหรือไม่
    ```bash
    git diff
    ```
3.  **Stage Files**: เลือกไฟล์ที่ต้องการบันทึก (แนะนำให้เลือกทีละไฟล์ถ้างานไม่เกี่ยวกัน)
    ```bash
    git add <ชื่อไฟล์>
    # หรือถ้ามั่นใจว่าทั้งหมดเกี่ยวกัน
    git add .
    ```
4.  **Confirm Status**: ตรวจสอบอีกครั้งว่าไฟล์เขียว (staged) ครบถ้วน
    ```bash
    git status
    ```
5.  **Commit**: บันทึกประวัติพร้อมข้อความสรุป (ดูตัวอย่างด้านล่าง)
    ```bash
    git commit -m "feat: complete next.js migration and rbac system"
    ```

---

## 📝 Git Commit Conventions (การเขียน Commit Message)

แนะนำรูปแบบ **Conventional Commits** (สั้น กระชับ เข้าใจง่าย):
- `feat`: (Feature) เพิ่มฟีเจอร์ใหม่
- `fix`: (Bug Fix) แก้ไขข้อผิดพลาด
- `refactor`: (Refactor) ปรับปรุงโค้ดโดยไม่เปลี่ยนผลลัพธ์
- `chore`: (Chore) งานจิปาถะ เช่น อัปเดต dependencies หรือ config
- `docs`: (Documentation) อัปเดตเอกสาร/Log

**ตัวอย่างสำหรับงานปัจจุบัน:**
- `feat: implement granular rbac and administrative tools`
- `fix: resolve typescript linting errors in settings actions`
- `docs: update project migration walkthrough and logs`

---

## 🏷️ Version Tagging (การทำ Version ก่อน Push)

การติด Tag ช่วยให้เราย้อนกลับมาหา Version ที่เสถียรได้ง่าย:

1.  **Create Tag**: สร้าง Tag แบบระบุข้อความ (Annotation)
    ```bash
    # รูปแบบ: git tag -a <version> -m "<message>"
    git tag -a v4.0.0-next -m "Complete Next.js migration with full feature parity"
    ```
2.  **Check Tags**: ตรวจสอบ List ของ Tags ทั้งหมด
    ```bash
    git tag
    ```

---

## 🚀 Pushing to Remote

1.  **Push Code**: ส่งโค้ดขึ้น Server
    ```bash
    git push origin main
    ```
2.  **Push Tags**: ส่ง Tag ขึ้น Server (Tag จะไม่ถูกส่งไปพร้อมกับ git push ปกติ)
    ```bash
    git push origin --tags
    ```

---
**ข้อแนะนำพิเศษ:** 
หากมีการแก้ไขไฟล์ `.env` อย่าลืมตรวจสอบว่าไฟล์นี้อยู่ใน `.gitignore` เพื่อไม่ให้ข้อมูลลับ (Secret Keys) หลุดขึ้นระบบ Git ครับ!
*แนะนำโดย Antigravity AI*
