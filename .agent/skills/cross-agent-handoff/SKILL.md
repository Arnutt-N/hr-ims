---
name: Cross-Agent Handoff
description: ระบบ Handoff สำหรับ AI Agents หลายตัว ทำงานร่วมกันข้ามแพลตฟอร์ม รับ-ส่งงาน อัพเดทสถานะ และรายงานปัญหา
---

# Cross-Agent Handoff System

ระบบนี้ทำให้ AI Agents หลายตัว (Antigravity, Kilo, Claude Code, Codex, Gemini CLI, Open Code ฯลฯ) สามารถทำงานร่วมกันบนโปรเจคเดียวกัน โดยมีกลไกรับ-ส่งงาน อัพเดทสถานะ และสื่อสารข้ามกัน

---

## 📌 Agent IDs

| Agent ID | Platform | Description |
|----------|----------|-------------|
| `antigravity` | Google Gemini (VS Code) | Antigravity AI Agent |
| `kilo` | Kilo Code (VS Code) | Kilo Code AI Agent |
| `claude_code` | Claude Code (CLI/VS Code) | Anthropic Claude Code |
| `codex` | OpenAI Codex (CLI) | OpenAI Codex Agent |
| `gemini_cli` | Gemini CLI | Google Gemini CLI |
| `open_code` | Open Code | Open source agent |

> ถ้ามี Agent ใหม่ ให้เพิ่มใน table นี้และใน `HANDOFF_BOARD.md`

---

## 📂 Folder Structure

```
project-log-md/
├── handoff/
│   ├── HANDOFF_BOARD.md              ← Dashboard กลาง (ทุก Agent ต้องอ่าน/อัพเดท)
│   └── logs/                         ← Handoff logs รายครั้ง
│       └── YYYY-MM-DD_HHmm_<from>_to_<to>.md
├── antigravity/                      ← Session logs ของ antigravity
├── kilo/                             ← Session logs ของ kilo
├── claude_code/                      ← Session logs ของ claude_code
├── codex/                            ← Session logs ของ codex
└── ...
```

---

## 🔄 Agent Lifecycle (ขั้นตอนการทำงาน)

ทุก Agent **ต้อง**ปฏิบัติตาม lifecycle นี้:

### 1. 📖 START — อ่าน Board ก่อนเสมอ

เมื่อเริ่มเซสชันใหม่ ให้ Agent:

```
1. อ่าน project-log-md/handoff/HANDOFF_BOARD.md
2. ดู Agent Status Table → ใครทำอะไรอยู่ มีงานค้างไหม
3. ดู Handoff Queue → มีงานส่งมาให้เราไหม
4. ดู Issues & Observations → มีปัญหาอะไรที่ต้องรู้ไหม
5. อ่าน handoff log ล่าสุดที่เกี่ยวข้อง (ถ้ามี)
```

### 2. 📥 RECEIVE — รับงานจาก Agent อื่น

ถ้ามี handoff ส่งมาหาเรา:

```
1. อ่าน handoff log file ใน handoff/logs/
2. ทำความเข้าใจ: งานเสร็จ / งานค้าง / งานส่งต่อ / ปัญหา
3. อัพเดท HANDOFF_BOARD.md:
   - เปลี่ยนสถานะตัวเองเป็น 🔵 IN_PROGRESS
   - ลบ entry ออกจาก Handoff Queue (ถ้ารับแล้ว)
   - เพิ่ม entry ใน Recent Activity Log
```

### 3. 🔨 WORK — ทำงาน

ทำงานตามปกติ บันทึก session log ไว้ใน folder ของตัวเอง:
- `project-log-md/<agent_id>/session_YYYY-MM-DD_HHmmss.md`

### 4. 📤 HANDOFF — ส่งมอบงาน

เมื่อจบเซสชัน หรือต้องส่งต่องาน:

```
1. สร้าง handoff log ใน handoff/logs/
   ชื่อไฟล์: YYYY-MM-DD_HHmm_<from>_to_<to>.md
   (ถ้าส่งให้ทุกคน ใช้ "all" แทน <to>)

2. อัพเดท HANDOFF_BOARD.md:
   - อัพเดทสถานะตัวเองเป็น 🟢 AVAILABLE หรือ 🟡 PENDING_HANDOFF
   - อัพเดท Last Active time
   - เพิ่ม/อัพเดท Current Sprint Tasks
   - เพิ่ม entry ใน Handoff Queue (ถ้าส่งต่อ Agent เฉพาะ)
   - เพิ่ม entry ใน Recent Activity Log
   - เพิ่ม Issues/Observations (ถ้ามี)
```

### 5. ⚠️ REPORT — รายงานปัญหา/ข้อแนะนำ

เมื่อพบปัญหาหรือมีข้อแนะนำ:

```
1. เพิ่มใน HANDOFF_BOARD.md → Issues & Observations table
2. ใส่รายละเอียดใน handoff log ด้วย
3. ถ้าเป็น blocker → เปลี่ยนสถานะตัวเองเป็น 🔴 BLOCKED
```

---

## 📝 Handoff Log Template

ใช้ template นี้เมื่อสร้าง handoff log ใหม่:

```markdown
# Handoff Log

| Field | Value |
|-------|-------|
| **Date** | YYYY-MM-DD HH:mm |
| **From Agent** | <agent_id> |
| **To Agent** | <agent_id> หรือ "all" |
| **Session Duration** | (ระบุเวลาที่ทำงาน หรือเว้นว่าง) |
| **Remark** | (หมายเหตุเพิ่มเติม ถ้ามี) |

---

## ✅ Completed Work (งานที่ทำเสร็จ)
1. รายการที่ 1
   - รายละเอียด
   - ไฟล์ที่แก้ไข: `path/to/file`
2. รายการที่ 2

## ⏳ Pending Work (งานที่ยังไม่ทำ)
- [ ] งาน 1
- [ ] งาน 2

## 📤 Handoff Tasks (งานที่ส่งต่อ)
- [ ] งานที่ต้องทำต่อ 1
  - Context: ...
  - Priority: 🔴 High / 🟡 Medium / 🟢 Low
- [ ] งานที่ต้องทำต่อ 2

## 📥 Received From (งานที่รับมา — ถ้ามี)
- รับมาจาก: <agent_id>
- Handoff log: [link to handoff log]
- สิ่งที่ทำต่อจากเดิม: ...

## ⚠️ Issues & Observations (ปัญหา/ข้อสังเกต/ข้อแนะนำ)
- 🐛 **Bug**: ...
- 💡 **Suggestion**: ...
- ⚠️ **Warning**: ...

## 📂 Files Changed
| Action | File Path | Description |
|--------|-----------|-------------|
| Modified | path/to/file | description |
| Created | path/to/new_file | description |
| Deleted | path/to/old_file | description |
```

---

## 📊 HANDOFF_BOARD.md Update Rules

เมื่ออัพเดท `HANDOFF_BOARD.md` ให้ปฏิบัติตามกฎเหล่านี้:

### Agent Status Table
- อัพเดทบรรทัดของ Agent ตัวเอง **เท่านั้น** (ห้ามแก้ของคนอื่น)
- อัพเดท: Status, Last Active, Current/Last Task, Session Log link
- ถ้าตัวเองเพิ่งเริ่ม → `🔵 IN_PROGRESS`
- ถ้าตัวเองเสร็จ → `🟢 AVAILABLE`
- ถ้ามีงานส่งต่อ → `🟡 PENDING_HANDOFF`
- ถ้าติดปัญหา → `🔴 BLOCKED`

### Current Sprint Tasks
- เพิ่ม/อัพเดท task ที่เกี่ยวข้องกับงานของตัวเอง
- เปลี่ยน status ของ task ที่ทำเสร็จเป็น `✅ Done`
- อย่าลบ task ที่ Agent อื่นเพิ่ม

### Handoff Queue
- เพิ่ม entry เมื่อส่งต่องาน
- ลบ entry เมื่อรับงานแล้ว

### Issues & Observations
- เพิ่ม entry ใหม่เมื่อพบปัญหา
- อัพเดท status เป็น `✅ Resolved` เมื่อแก้แล้ว

### Recent Activity Log
- เพิ่ม entry ใหม่ **ด้านบนสุด** ของ table (ใหม่สุดอยู่บน)
- เก็บไว้ไม่เกิน **10 รายการล่าสุด** (ลบเก่าสุดออก)

---

## 🕐 Custom Time / Manual Time Entry

Agent สามารถ **กำหนดเวลาเอง** ได้ในกรณี:
- ทำงาน offline แล้วมาบันทึกทีหลัง
- User ระบุเวลาให้
- แก้ไขเวลาผิดพลาด

วิธีใช้: ระบุเวลาใน field `Date` และ `Remark` ของ handoff log

---

## 🔗 Integration with Session Summary

ระบบ Handoff ทำงานร่วมกับ `/session-summary` workflow:
- `/session-summary` → บันทึก session log ของ Agent ตัวเองใน folder ของตัวเอง
- `/agent_handoff` → สร้าง handoff log + อัพเดท Board สำหรับส่งต่องาน

**แนะนำ:** รัน `/session-summary` ก่อน แล้วค่อยรัน `/agent_handoff`

---

## ⚡ Quick Reference

| ต้องการ | ทำอย่างไร |
|---------|-----------|
| เริ่มต้นงาน | อ่าน `HANDOFF_BOARD.md` → ดู status ทุก Agent |
| รับงาน | อ่าน handoff log ใน `handoff/logs/` → อัพเดท Board |
| ส่งงาน | สร้าง handoff log → อัพเดท Board |
| รายงานปัญหา | เพิ่มใน Board → Issues table |
| ดูประวัติ | ดู `handoff/logs/` หรือ Recent Activity Log |
| ดู session เก่า | ดูใน `project-log-md/<agent_id>/` |
